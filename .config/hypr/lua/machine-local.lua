-- Sensibilidad de puntero por máquina (trackeado en el repo, sincronizado).
-- Detección: ratón externo presente -> sensitivity_mouse; solo touchpad -> sensitivity_touchpad.
-- Ajusta los valores en cada máquina si quieres un tacto distinto; las demás
-- heredan estos defaults.
return { sensitivity_mouse = -0.8, sensitivity_touchpad = 0 }